# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# ShipStudio metadata
.shipstudio/

# Notion-baked insights content (content/insights/, public/insights/) is
# committed on purpose: the repo stays self-contained — a clone builds the full
# site without NOTION_TOKEN. scripts/notion-sync.mjs refreshes it each build;
# commit the refreshed files when posts change.
